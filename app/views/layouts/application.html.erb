<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Cv Builder" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <nav class="flex justify-between items-center py-4 px-8 bg-white border-b border-gray-200 mb-8">
      <%= link_to "CVBuilder", root_path, class: "text-2xl font-bold no-underline text-inherit" %>

      <div class="flex gap-4 items-center">
        <% if user_signed_in? %>
          <%= link_to "Dashboard", dashboard_path, class: "no-underline py-2 px-4 bg-blue-500 text-white rounded hover:bg-blue-600" %>
          <span><%= current_user.email %></span>
          <%= button_to "Log Out", destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "no-underline py-2 px-4" %>
          <%= link_to "Signup", new_user_registration_path, class: "no-underline py-2 px-4" %>
        <% end %>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>
