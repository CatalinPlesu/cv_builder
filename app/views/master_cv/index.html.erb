<div class="flex">
  <%= render 'sidebar_tags', tags: @tags %>

  <!-- Main Content -->
  <div class="flex-1 p-6">
    <%= render 'section_container', 
                heading_text: "Heading", 
                heading_color: "green-500", 
                border_color: "green-500" do %>
      <%= render "heading", locals: { full_name: @full_name, heading: @heading } %>
    <% end %>
  </div>
</div>

